// Multiplication Table: Create a program that generates the multiplication table for a given number N using a for loop, printing the results for 1 to 10.

let n = 1;

for (let i = 1; i <= 10; i++) {
    let result = n * i;
    console.log(`${n} x ${i} = ${result}`);
}
